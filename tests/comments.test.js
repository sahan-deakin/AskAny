const request = require('supertest')
const app = require('../src/app')
const db = require('../src/db')

// placeholder - full comment tests added in next step when comments feature is built
describe('Comments Routes', () => {
    it('placeholder - comment tests coming in next step', () => {
        expect(true).toBe(true)
    })
})
